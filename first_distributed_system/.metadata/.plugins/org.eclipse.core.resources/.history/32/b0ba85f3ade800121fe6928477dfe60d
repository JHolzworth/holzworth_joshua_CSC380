package Server;

import java.net.ServerSocket;

public class ServerApplication {

	ServerSocket serverSocket;
	
	public ServerApplication(int socketNumber){
		serverSocket = new ServerSocket(socketNumber);
	}
}
